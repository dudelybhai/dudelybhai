name: Waka Readme

on:
  workflow_dispatch:
  schedule:
    # Runs at 12am UTC
    - cron: "0 0 * * *"

jobs:
  update-readme:
    name: Update this repo's README
    runs-on: ubuntu-latest
    steps:
      - uses: athul/waka-readme@master
        with:
          WAKATIME_API_KEY: ${{ secrets.c1331d26-d1d2-4f09-9d85-b08fa1ab47f1 }}
          GH_TOKEN: ${{ secrets.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 }}
          SHOW_TITLE: true
